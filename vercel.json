{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "npm install" },
    { "src": "src/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/server.ts" },
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/favicon.ico", "dest": "/favicon.ico" }
  ]
}
